<h2><%= link_to question.title, question %></h2> 

<h4><strong>Topics:</strong> <% question.tags.each do |tag| %> 
  <%= tag.name %>
  <% end %></h4>
  <h4>Asked by <%= question.user.name %> </h4>

  </p>



<% if current_user %>
  <p><%= link_to 'Edit', edit_question_path(question) %> | 
<%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></p>
<% else %>
  <p> <%= link_to "You must log in to edit or delete this question.", github_login_path %> </p>
<% end %>
 <img src="divider.png" width="200">